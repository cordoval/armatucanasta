{% extends 'GicsdrArmatucanastaBundle::base.html.twig' %}

{% block title %}Lista de Conversión{% endblock %}

{% block body %}
<h1>Editar conversión</h1>

<form action="{{ path('conversion_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Editar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('conversion') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <form action="{{ path('conversion_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Borrar</button>
        </form>
    </li>
</ul>
{% endblock %}
