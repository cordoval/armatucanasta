{% extends 'GicsdrArmatucanastaBundle::base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<h1>Unidad</h1>

<div class="row">
            <div class="span2">Id</div>
            <div class="span6">Descripción</div>
            <div class="span4">Descbreve</div>
            <div class="span2">{{ entity.id }}</div>
            <div class="span6">{{ entity.descripcion }}</div>
            <div class="span4">{{ entity.descbreve }}</div>
</div>

<ul class="record_actions">
    <li>
        <a href="{{ path('unidad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('unidad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('unidad_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
